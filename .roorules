# 🏛️ SIDS 协同进化协议 (Refined .roorules)

## 一、 核心架构契约 (Invariants)
> AI 必须始终守护的架构底座，不容妥协。

1. **存算分离一致性**：所有数据读写必须通过 `/app/data` 挂载点，严禁将数据写入容器 Root FS。
2. **特征物理锁**：无论是 Spark ETL 还是 FastAPI 推理，必须强制执行 `CORE_FEATURES` 列对齐，这是防止“沉默失败”的最高优先级任务。
3. **ARM64 原生性**：禁止生成任何依赖特定 x86 指令集的库或优化，确保在 M2 芯片上无缝运行。

## 二、 动态思维：防御性编程原则 (Philosophy)

1. **先校验，后断言**：在操作非空目录或大型文件前，AI 需自主决定检查手段（如 `ls` 或 `head`），目的是确保 Schema 的真实性，而非履行审计形式。
2. **跨计算引擎对齐**：当你在 Spark 环境写代码时，必须考虑 DuckDB 读取时的兼容性（如 Parquet 分片逻辑）；反之亦然。
3. **最小惊扰提交**：Git Commit 必须是原子性的。如果你改了 ETL 逻辑又顺手改了 UI，请提醒用户分两次提交。

## 三、 协作红线 (Hard Limits)
1. **数据泄露防御**：严禁将任何 >1MB 的非代码文件加入 `git add` 列表。
2. **禁止虚构 Diff**：在生成 Commit Message 前，AI 必须通过实际读取 Git Staging 差异来总结，严禁凭记忆编造。

## 四、 自检与透明度 (Self-Check)
> 拒绝死板的格式，提倡“关键决策同步”。

- **思考同步 (Thinking)**：不再要求汇报琐碎操作，仅需同步：**“我发现了什么冲突/异常 -> 我打算如何适配架构契约”**。