SIDS：业务系统升级方案
版本：V3.0（学术强化与业务闭环融合版）

---
一、 重构背景与系统定位
原系统主要聚焦于数据清洗与模型训练，属于“数据分析型系统”，缺乏完整的业务流程，无法满足软件工程类毕业设计对于“系统性、交互性、业务闭环”的要求()。  

附加的业务系统核心目标不是构建复杂的物理真实网络攻击环境，而是：在可控的工程环境中验证模型对“真实未知数据”的检测能力，并提供包含告警、工单、策略在内的企业级安全运营业务闭环，进行可视化呈现()。  

---
二、 核心设计原则（答辩关键与学术基石）
本系统严格遵循三条核心原则，以应对答辩中可能出现的质疑：
1. 数据真实性（Data Authenticity）：所有输入数据均来源于公开权威数据集（CSE-CIC-IDS2018），严格采用训练集 / 测试集隔离机制()。仿真阶段仅使用未参与训练的测试集数据（Hold-out Testing），绝不存在人工构造虚假数据()。  
2. 
3. 时间一致性（Temporal Consistency）：系统不采用完全随机采样，而是基于时间窗口进行数据切片回放()。这保证了攻击行为的连续性、流量波动的真实性，确保时间序列特征不被破坏()。  
4. 
5. 模型独立性（Model Independence）：可视化层（地图/IP）与业务层面的逻辑节点仅用于工程化展示，绝不参与模型计算()。所有检测预测结果仅基于流量特征向量()。  
6. 

---
三、 系统总体架构
系统采用清晰的分层架构，实现从数据底层到业务可视化的完整闭环：
数据层 → 流量回放引擎 → 检测与预测层 → 业务运营层 → 态势可视化层
- 数据层：采用 DuckDB 实现实时数据存储、Parquet 实现离线数据存储（CSE-CIC-IDS2018）()。训练集占 80%，测试集占 20%（用于仿真回放）()。  
- 
- 流量回放引擎：替代传统靶机发包，基于时间窗口的测试集数据切片回放，按微批形式向后端推送()。  
- 
- 检测与预测层：基于 XGBoost 模型进行毫秒级流量分类推理()；基于 EWMA 算法进行时间序列趋势建模，输出未来攻击风险等级()。  
- 
- 业务运营层（重点扩充）：涵盖告警管理、工单流转、策略配置等核心业务模块，补足软件工程业务闭环要求()。  
- 
- 态势可视化层：提供动态中国地图态势图、多维趋势分析图以及角色权限控制的可视化面板()。  
- 

---
四、 核心业务与功能模块设计
4.1 流量回放引擎（核心驱动，替代发包）
这是保障系统运行的最核心模块，彻底规避底层物理网络抓包的复杂依赖。
- 核心机制：基于时间窗口的数据切片回放（Time-window Replay）()。  
- 
- 工作流程：从测试集中提取连续时间片（如 5~10 秒窗口），按顺序加载数据，并以微批形式（窗口大小 500ms，每批 30~50 条）推送到后端()。  
- 
- 流量混合策略：支持前端设置“攻击强度”，按照比例将 Normal 流量与 Attack 流量混合推送（例如：20% 攻击流量 + 80% 正常流量）()。  
- 
4.2 实时攻击检测与趋势预测模块
- 多分类检测：基于 XGBoost 算法，输入 15 维统计特征向量，输出攻击类型（Normal / DoS / BruteForce 等）与置信度()。  
- 
- 趋势预测（加分项）：采用指数加权移动平均（EWMA）算法进行短期趋势预测()。预测时间窗口锁定为未来5分钟()。  
- 
  - 预测算法公式：$S_t = \alpha X_t + (1-\alpha)S_{t-1}$()。  
  - 
  - 当预测值超过当前值设定阈值或呈现连续上升趋势时，触发高级别预警()。  
  - 
4.3 实时态势感知模块（前端大屏）
基于地图的大屏展示，结合逻辑探针节点（Logical Probe Nodes）模拟分布式攻击场景，遵循“三层视觉展现”原则()：  
- 第一层（视觉核心）：高频闪烁的实时告警列表、同步跳动的攻击统计数字、当前在线节点数()。  
- 
- 第二层（业务核心）：动态双折线图（实线为历史攻击频率，虚线为基于 EWMA 算法的未来 5 分钟预测趋势），以及多维趋势分析图（各类攻击趋势）()。  
- 
- 第三层（氛围装饰）：构建固定节点池（50个虚拟节点），绑定虚拟IP与中国城市坐标()。地图节点根据状态变色（Normal蓝色 / DoS黄色 / BruteForce红色），声明：地理信息与节点仅用于可视化表达，不参与计算()。  
- 
4.4 安全运营业务模块（补足工程工作量）
为满足软件工程闭环要求，采用轻量级策略实现以下业务模块，避免过度干扰核心链路性能()：  
- 告警管理：展示实时告警列表，包含时间、逻辑来源IP、攻击类型、置信度、处理状态()。  
- 
- 工单处理：实现基础的“告警 → 工单创建 → 分配 → 处理 → 关闭”的状态流转列表展示()。  
- 
- 策略与监控管理：支持设置检测阈值、黑白名单IP管理、预测预警阈值设置()。同时监控系统 QPS、推理延迟、WebSocket连接数等工程指标()。  
- 
- 用户与权限系统：实现极简的注册、登录与 JWT 鉴权功能()。角色划分为管理员、安全分析员、观察者，进行粗粒度访问控制()。  
- 

---
五、 系统核心业务流（答辩演示脚本）
1. 用户登录与鉴权：用户通过账号密码登录系统，获取 JWT 令牌进入态势大屏()。  
2. 
3. 启动回放仿真：选择攻击类型与强度参数，启动流量回放引擎()。  
4. 
5. 流式推理与预测：系统加载测试集时间片，按微批推送，XGBoost 模型实时输出检测分类结果，EWMA 模型同步计算未来趋势()。  
6. 
7. 业务闭环与响应：告警管理模块实时捕获异常数据并生成告警日志，管理员可一键将其转为处理工单()。  
8. 
9. 可视化态势呈现：前端 WebSocket 实时接收推送，地图节点动态变色，折线图实时延展并高亮预警提示()。  
10. 

---
六、 技术栈选型
- 后端支撑：FastAPI + XGBoost（模型推理） + Python（EWMA 时间序列分析）()。  
- 
- 数据层：DuckDB + Parquet（高性能读写与测试集加载）()。  
- 
- 仿真与通信：Python 脚本（时间窗口微批回放引擎） + WebSocket（实时全双工推送）()。  
- 
- 前端展现：Vue3 + ECharts（态势大屏与后台管理）()。  
- 

---
七、 核心创新与总结（论文提炼点）
1. 基于测试集的真实流量时间窗口回放机制：彻底摒弃人工构造或靶机发包，在不依赖底层庞大物理网络环境的前提下，保留了时间序列特征，保证了验证数据的学术真实性()。  
2. 
3. 逻辑节点映射的分布式可视化机制：巧妙通过局部状态映射，实现了空间维度的分布式攻击态势展示，兼顾了视觉冲击力与模型独立性()。  
4. 
5. 检测 + 预测 + 业务运营的一体化闭环：将单纯的分类模型升级为具备“未来5分钟趋势预警”能力的业务平台，并引入告警、工单等SOC模块，兼具学术严谨性与软件工程的落地实用价值()。  